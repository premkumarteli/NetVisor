"""Compatibility shim for shared domain utility helpers."""

from intel.domain_utils import _EXTRACTOR, get_base_domain, normalize_host

__all__ = ["get_base_domain", "normalize_host", "_EXTRACTOR"]

